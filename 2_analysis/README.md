# 2_analysis

For this illustration-only figure, "analysis" means deterministic figure packaging:

1. verify source artwork hashes,
2. copy canonical PDF/PNG,
3. convert PDF to SVG,
4. document provenance in `provenance/runs.jsonl`.

No statistical table-generation pipeline is required for this figure.
