# Provenance log

`runs.jsonl` is append-only and records each artifact-producing run.

For Figure 1 (illustration-only), provenance emphasizes:
- canonical source-artwork pointers,
- deterministic conversion/packaging commands,
- output hashes.
